-- spectra.nvim plugin loader
-- This file is loaded automatically by Neovim

if vim.g.loaded_spectra then
  return
end
vim.g.loaded_spectra = true

-- The plugin is lazy-loaded, setup must be called explicitly
-- require("spectra").setup({})

